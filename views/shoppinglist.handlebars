{{!-- Store the ingredients from shoppingRoutes.js in a card --}}
<h5 class="card-title">Shopping List</h5>
{{#each ingredients}}
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{this.name}}</h5>
            <!-- Other card content... -->
        </div>
    </div>
{{/each}}



