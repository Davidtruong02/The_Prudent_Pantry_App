<!-- shoppinglist.handlebars -->

{{#each ingredients}}
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{this.name}}</h5>
        </div>
    </div>
{{/each}}